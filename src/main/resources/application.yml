spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

server:
  port: 8080

claude:
  api-key: ${CLAUDE_API_KEY:your-api-key-here}
  model: claude-sonnet-4-6
  max-tokens: 4096
